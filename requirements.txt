# Web framework
fastapi>=0.118,<1.0
uvicorn[standard]>=0.34,<1.0

# LangGraph / LangChain (both on the 1.x line as of mid-2026)
langgraph>=1.2,<2.0
langchain-core>=1.4,<2.0
langchain-groq>=1.1,<2.0

pydantic>=2.10,<3.0

# Score persistence
supabase>=2.15,<3.0
python-dotenv>=1.0,<2.0

# Optional but required for correctness on serverless deployments
# (see backend/app/graph.py -- persistent HITL checkpoints via Postgres).
# psycopg 3.3.2+ ships cp314 wheels; pin at least that floor.
langgraph-checkpoint-postgres>=2.0,<4.0
psycopg[binary,pool]>=3.3.2,<4.0
